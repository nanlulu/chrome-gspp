node_modules/

# Unpacked build — load this folder via chrome://extensions. Regenerated by
# `npm run build`, so it is never committed.
dist/

# Store upload packages, one per version. Kept locally rather than in git;
# attach them to a GitHub Release if you want them shared.
releases/

.DS_Store
